# MediShelf AI — backend + ML runtime
#
# Install the CPU build of PyTorch first, otherwise pip pulls the large CUDA wheels:
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r backend/requirements.txt
#
# Versions are pinned. scikit-learn in particular MUST stay on 1.9.x: the storage-risk
# model in ml/artifacts/storage_risk/model.joblib was fitted with 1.9.0, and loading it
# under an older release raises InconsistentVersionWarning.

# --- API layer ---
fastapi==0.118.0
uvicorn[standard]==0.37.0
python-multipart==0.0.20
pydantic==2.11.9
pydantic-settings==2.11.0
python-dotenv==1.1.1

# --- persistence ---
sqlalchemy==2.0.43

# --- numerics / classical ML (storage-risk model) ---
numpy==2.2.6
pandas==2.3.3
scikit-learn==1.9.0
joblib==1.5.2

# --- deep learning (package classifier) ---
torch==2.6.0
torchvision==0.21.0
pillow==12.3.0

# --- OCR pipeline ---
# EasyOCR weights are NOT bundled. The extractor runs with download_enabled=False, so
# fetch them once before first use:
#   python -c "import easyocr; easyocr.Reader(['en'], gpu=False, download_enabled=True)"
# Without the weights the scan endpoint still works and reports ocr_status="unavailable".
easyocr==1.7.2
opencv-python-headless==4.12.0.88

# --- tests ---
pytest==8.4.2
httpx==0.28.1
